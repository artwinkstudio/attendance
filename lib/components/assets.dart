import 'package:flutter/material.dart';

final logoImage = Image.asset(
  'images/logo.jpg',
  height: 200.0,
  width: 200.0,
  fit: BoxFit.cover,
);

const adminDivider = Divider(thickness: 5, color: Colors.deepPurple);
